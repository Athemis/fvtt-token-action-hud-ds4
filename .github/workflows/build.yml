name: Build
permissions:
  contents: read

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22

      - name: Install dependencies
        run: npm ci || npm install

      - name: Build
        run: npm run build

      - name: Verify Build Artifacts
        run: |
          test -f scripts/token-action-hud-ds4.min.js
          test -f scripts/token-action-hud-ds4.min.js.map

      - name: Test
        run: npm test

      - name: Lint (optional)
        run: npm run lint
        continue-on-error: true
